# Google Places API key — used to fetch live rating/review count in the Reviews section.
# Get a key at https://console.cloud.google.com/ and enable the Places API (New).
# If omitted, the component falls back to hardcoded values (5.0 / 37).
VITE_GOOGLE_API_KEY=your_api_key_here
